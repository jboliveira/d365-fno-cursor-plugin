---
name: xpp-fno-plan
description: Create a structured D365 F&O X++ implementation plan before coding.
---

Run the **xpp-fno-plan** skill workflow:

1. Load skill `xpp-fno-plan` and hub `xpp-fno-development`.
2. Apply scenario router rule `xpp-fno-scenario-router`.
3. Output requirement summary, artifact table, quality gates, risks, and next step (`/xpp-fno-implementer`).

Planning only — do not edit code unless user asks to save the plan to a file.

Arguments: $ARGUMENTS
